from setuptools import setup

setup(
    name="ball_beam",
    version='0.0.1',
    install_requires=['gym',
                      'pybullet',
                      'numpy',
                      'matplotlib',
                      'pandas',
                      'seaborn']
)